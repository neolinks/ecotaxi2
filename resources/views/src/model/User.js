/**
 * Created by ernar on 13-Jan-16.
 */
export class User{
    static id;
    static name;
    static company;
    static address;
    static number;
    static phones;
    static token;

    set id(id){ this.id = id; }
    get id() { return this.id; }
    set name(name){ this.name = name; }
    get name() { return this.name; }
    set company(company){ this.company = company; }
    get company() { return this.company; }
    set address(address){ this.address = address; }
    get address() { return this.phones; }
    set number(number){ this.phones = phones; }
    get number() { return this.phones; }
    set phones(phones){ this.id = phones; }
    get phones() { return this.phones; }
    set token(token){ this.token = token; }
    get token() { return this.token; }

}